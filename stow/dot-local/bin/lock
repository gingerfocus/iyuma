#!/bin/sh

swaylock &
lockid=$!

# TODO: invert condition to do timeout by default, idle type script

# TODO: change brightness first
if [ "$1" == "sleep" ]; then
    # todo, set up something where you change the brightness and restore it after
    # sleep
    # TODO: record when I go to sleep each night
    #
    # TODO: setup something to check if batter hits a certain threash hold and then sleep before that
    sleep 3600
fi

# TODO: if unlock happens before timer then cancel and reset. only lock when
# timer ends

systemctl suspend

wait $lockid

# echo "Resuming bluetooth... (i dont know why this is needed)"
# systemctl restart bluetooth-rfkill-resume.service
# rfkill unblock bluetooth

